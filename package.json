{
  "name": "beauty-radar-product-cloud",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/materialize-numbuzin-no5-image.mjs && vite",
    "build": "node scripts/materialize-numbuzin-no5-image.mjs && vite build && node scripts/remove-retired-products.mjs && node scripts/validate-dist.mjs",
    "preview": "vite preview",
    "check:data": "node scripts/validate-products.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
